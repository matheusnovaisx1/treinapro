import { LoadingAnimation } from '@/components/ui/loading-animation';

// Exibido durante a navegação entre as páginas do aluno.
export default function AlunoLoading() {
  return <LoadingAnimation label="Preparando seu treino…" />;
}
